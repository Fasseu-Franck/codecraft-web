// Basé sur guide-installation-shadcn-ui.md - cn helper function
import { clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs) {
  return twMerge(clsx(inputs))
}
